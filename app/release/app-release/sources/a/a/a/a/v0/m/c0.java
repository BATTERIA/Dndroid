package a.a.a.a.v0.m;

import e.f.a.g;
import java.util.Comparator;

public final class c0<T> implements Comparator<T> {
    @Override // java.util.Comparator
    public final int compare(T t, T t2) {
        return g.a(t.toString(), t2.toString());
    }
}
